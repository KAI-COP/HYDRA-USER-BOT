# 🐉 Hydra UserBot

**Hydra UserBot** — это современный, быстрый и модульный юзербот для Telegram, построенный на базе библиотеки **Telethon**. Он спроектирован для легкой кастомизации и эффективной автоматизации личного аккаунта.

---

## ✨ Особенности

* **Модульная архитектура**: Система автоматически загружает файлы `.py` из папки `modules`, регистрируя команды и справку.
* **Умная обработка сообщений**: Встроенная функция `edit_or_reply` автоматически решает, редактировать ли ваше сообщение или отвечать пользователю.
* **Защита от спама**: Встроенный декоратор `rate_limit` для предотвращения блокировок аккаунта.
* **Система помощи**: Динамическая генерация справки по командам через модули.
* **Асинхронность**: Высокая производительность благодаря `asyncio`.

## 🛠 Требования

* Python 3.8+
* `api_id` и `api_hash` (получить на [my.telegram.org](https://my.telegram.org))
* Библиотека `Telethon`

## 🚀 Быстрый старт

### 1. Установка

```bash
git clone https://github.com/KAI-COP/HYDRA-USER-BOT.git
cd HYDRA-USER-BOT
pip install -r requirements.txt

```

### 2. Настройка

Отредактируйте файл `config.py`, указав свои данные:

* `api_id`: Ваш API ID.
* `api_hash`: Ваш API Hash.
* `prefix`: Префикс для команд (например, `.`).

### 3. Запуск

```bash
python main.py

```

При первом запуске бот попросит ввести номер телефона и код подтверждения (а также пароль 2FA, если он установлен).

---

## 🏗 Разработка модулей

Создать свой модуль очень просто. Бот автоматически распознает функции как команды.

**Пример (modules/hello.py):**

```python
from utils.misc import edit_or_reply

# Бот автоматически зарегистрирует команду .hello
async def hello(event):
    await edit_or_reply(event, "👋 Привет от Hydra UserBot!")

# Добавление описания в .help
modules_help = {
    "hello": {
        "hello": "Поздороваться с пользователем"
    }
}

```

---

## 📚 Основные команды

* `.help` — Список всех модулей.
* `.help [название]` — Подробная информация о командах модуля.
* `.find [запрос]` — Поиск команды по описанию.

---

## 📂 Структура проекта

* `main.py` — Точка входа и инициализация клиента.
* `modules/` — Папка для ваших кастомных модулей.
* `utils/` — Вспомогательные утилиты:
* `loader.py`: Динамическая загрузка модулей.
* `misc.py`: Функции времени работы (uptime), лимиты и работа с сообщениями.


* `user_settings.json` — Файл для хранения настроек пользователей.